import 'package:flutter/material.dart';

/// Paleta de NextHappen, tomada de tu panel web (Vue) para mantener
/// la misma identidad visual en la app móvil: fondo crema, bordes negros
/// marcados y acento amarillo.
class AppColors {
  AppColors._();

  static const Color primaryYellow = Color(0xFFFFC629);
  static const Color background = Color(0xFFFFF8ED);
  static const Color black = Color(0xFF161616);
  static const Color lightGreen = Color(0xFFD9F2E1);
  static const Color grey = Color(0xFFEDE8DC);
  static const Color error = Color(0xFFD64545);
}
